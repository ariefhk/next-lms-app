import AppLayout, { AppLayoutProps } from "@/views/components/layout/app-layout"

const Layout = ({ children }: AppLayoutProps) => {
  return <AppLayout>{children}</AppLayout>
}

export default Layout
